    <form action="/users/search" method="post">
        姓名：<input type="text" name="s_name" value="{{s_name}}"><br> 年龄：
        <input type="text" name="s_age" value="{{s_age}}"><br>
        <input type="submit" value="查询">
    </form>

 {{title}}
    <table>
        <tr>
            <th>编号</th>
            <th>姓名</th>
            <th>年龄</th>
            <th>操作</th>
        </tr>
        {{#each data}}
        <tr>
            <td>{{id}}</td>
            <td>{{name}}</td>
            <td>{{age}}</td>
            <td><a href="/users/add">新增</a></td>
            <td><a href="/users/del/{{id}}">删除</a></td>
            <td><a href="/users/toUpdate/{{id}}">修改</a></td>
        </tr>
        {{/each}}
    </table>